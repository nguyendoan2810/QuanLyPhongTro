<!-- Modal thêm phòng -->
<div id="roomCreateModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden flex items-center justify-center p-4 modal-backdrop">
    <div class="bg-white rounded-xl max-w-lg w-full max-h-[90vh] overflow-y-auto shadow-lg bounce-in">
        <!-- Header -->
        <div class="p-6 border-b border-gray-200 flex justify-between items-center">
            <h3 class="text-lg font-semibold text-gray-800">Thêm phòng mới</h3>
            <button onclick="closeModal('roomCreateModal')" class="text-gray-400 hover:text-gray-600">
                <i class="fas fa-times text-xl"></i>
            </button>
        </div>

        <!-- Form nội dung -->
        <div class="p-6">
            <form id="createPhongForm" class="space-y-4">
                @Html.AntiForgeryToken()

                <!-- Tên phòng -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Tên phòng *</label>
                    <input type="text" name="TenPhong" class="form-input w-full px-3 py-2 rounded-lg border-gray-300 focus:ring-2 focus:ring-blue-500" placeholder="Nhập tên phòng" required>
                </div>

                <!-- Giá phòng -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Giá phòng (VNĐ) *</label>
                    <input type="number" name="GiaPhong" min="0" class="form-input w-full px-3 py-2 rounded-lg border-gray-300 focus:ring-2 focus:ring-blue-500" placeholder="Nhập giá phòng" required>
                </div>

                <!-- Trạng thái -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Trạng thái</label>
                    <select name="TrangThai" class="form-input w-full px-3 py-2 rounded-lg border-gray-300 focus:ring-2 focus:ring-blue-500">
                        <option value="Trống">Trống</option>
                        <option value="Đang thuê">Đang thuê</option>
                    </select>
                </div>

                <!-- Tầng -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Tầng</label>
                    <input type="text" name="Tang" class="form-input w-full px-3 py-2 rounded-lg border-gray-300 focus:ring-2 focus:ring-blue-500" placeholder="VD: Tầng 1">
                </div>

                <!-- Loại phòng -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Loại phòng</label>
                    <input type="text" name="LoaiPhong" class="form-input w-full px-3 py-2 rounded-lg border-gray-300 focus:ring-2 focus:ring-blue-500" placeholder="VD: Đơn / Đôi">
                </div>

                <!-- Diện tích -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Diện tích (m²)</label>
                    <input type="number" name="DienTich" min="0" step="0.1" class="form-input w-full px-3 py-2 rounded-lg border-gray-300 focus:ring-2 focus:ring-blue-500" placeholder="VD: 25">
                </div>

                <!-- Địa chỉ -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Địa chỉ</label>
                    <input type="text" name="DiaChi" class="form-input w-full px-3 py-2 rounded-lg border-gray-300 focus:ring-2 focus:ring-blue-500" placeholder="Nhập địa chỉ phòng trọ">
                </div>

                <!-- Mô tả -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Mô tả</label>
                    <textarea name="MoTa" rows="3" class="form-input w-full px-3 py-2 rounded-lg border-gray-300 focus:ring-2 focus:ring-blue-500" placeholder="Nhập mô tả thêm..."></textarea>
                </div>
            </form>
        </div>

        <!-- Footer -->
        <div class="p-6 border-t border-gray-200 flex justify-end space-x-3">
            <button onclick="closeModal('roomCreateModal')" class="px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition">
                Hủy
            </button>
            <button type="submit" form="createPhongForm" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">
                <i class="fas fa-save mr-2"></i>Lưu
            </button>
        </div>
    </div>
</div>