<div id="viewTenantModal" class="modal fixed inset-0 z-50 hidden flex items-center justify-center bg-black bg-opacity-50">
    <div class="modal-content bg-white rounded-lg shadow-xl w-full max-w-lg">
        <div class="p-6 border-b border-gray-200 flex justify-between items-center">
            <h3 class="text-lg font-semibold text-gray-800">Thông tin chi tiết khách thuê</h3>
            <button onclick="closeModal('viewTenantModal')" class="text-gray-400 hover:text-gray-600">
                <i class="fas fa-times"></i>
            </button>
        </div>

        <div class="p-6 space-y-4">
            <div id="viewTenantDetails">
                <p class="text-center text-gray-500">Đang tải dữ liệu...</p>
            </div>
        </div>

        <div class="p-6 bg-gray-50 border-t border-gray-200 flex justify-end">
            <button type="button" onclick="closeModal('viewTenantModal')" class="btn-secondary px-4 py-2 rounded-lg">Đóng</button>
        </div>
    </div>
</div>