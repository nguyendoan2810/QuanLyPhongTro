<!-- Modal xem thông tin khách thuê -->
<div id="viewTenantModal"
     class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden flex items-center justify-center p-4 modal-backdrop">
    <div class="bg-white rounded-xl max-w-lg w-full shadow-lg overflow-hidden bounce-in">
        <!-- Header -->
        <div class="p-6 border-b border-gray-200 flex justify-between items-center">
            <h3 class="text-lg font-semibold text-gray-800 flex items-center gap-2">
                <i class="fas fa-user text-blue-600"></i> Thông tin chi tiết khách thuê
            </h3>
            <button onclick="closeModal('viewTenantModal')" class="text-gray-400 hover:text-gray-600 transition">
                <i class="fas fa-times text-xl"></i>
            </button>
        </div>

        <!-- Nội dung -->
        <div class="p-6 space-y-5 bg-white">
            <div id="viewTenantDetails" class="space-y-3 text-gray-700">
                <p class="text-center text-gray-500 italic">Đang tải dữ liệu...</p>
                <!-- Khi có dữ liệu, JS sẽ thay nội dung vào đây -->
            </div>
        </div>

        <!-- Footer -->
        <div class="p-5 border-t border-gray-200 bg-gray-50 flex justify-end">
            <button type="button"
                    onclick="closeModal('viewTenantModal')"
                    class="px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition">
                Đóng
            </button>
        </div>
    </div>
</div>