<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hệ thống Quản lý Phòng trọ</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/quanly.css">
    <script src="~/js/quanly.js"></script>
</head>
<body>
    <!-- Login Page -->
    <!--Trang đăng nhập-->
    <div id="loginPage" class="login-container flex items-center justify-center p-4">
        <div class="login-card rounded-2xl shadow-2xl p-8 w-full max-w-md bounce-in">
            <div class="text-center mb-8">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-blue-600 rounded-full mb-4">
                    <i class="fas fa-building text-white text-2xl"></i>
                </div>
                <h1 class="text-3xl font-bold text-gray-800 mb-2">Quản lý Phòng trọ</h1>
                <p class="text-gray-600">Đăng nhập để tiếp tục</p>
            </div>

            <form id="loginForm" class="space-y-6">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Email hoặc Tên đăng nhập</label>
                    <input type="text" id="loginUsername" class="form-input w-full px-4 py-3 rounded-lg focus:outline-none" placeholder="Nhập email hoặc tên đăng nhập" required>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Mật khẩu</label>
                    <div class="relative">
                        <input type="password" id="loginPassword" class="form-input w-full px-4 py-3 rounded-lg focus:outline-none pr-12" placeholder="Nhập mật khẩu" required>
                        <button type="button" onclick="togglePassword('loginPassword')" class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-500 hover:text-gray-700">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                </div>

                <div class="flex items-center justify-between">
                    <label class="flex items-center">
                        <input type="checkbox" class="rounded border-gray-300 text-blue-600 focus:ring-blue-500">
                        <span class="ml-2 text-sm text-gray-600">Ghi nhớ đăng nhập</span>
                    </label>
                    <a href="#" class="text-sm text-blue-600 hover:text-blue-800">Quên mật khẩu?</a>
                </div>

                <button type="submit" class="btn-primary w-full py-3 text-white font-semibold rounded-lg">
                    <i class="fas fa-sign-in-alt mr-2"></i>Đăng nhập
                </button>

                <div class="text-center">
                    <span class="text-gray-600">Chưa có tài khoản? </span>
                    <button type="button" onclick="showRegister()" class="text-blue-600 hover:text-blue-800 font-medium">Đăng ký ngay</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Register Page -->
    <!--Trang đăng ký-->
    <div id="registerPage" class="login-container flex items-center justify-center p-4 hidden">
        <div class="login-card rounded-2xl shadow-2xl p-8 w-full max-w-md bounce-in">
            <div class="text-center mb-8">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-blue-600 rounded-full mb-4">
                    <i class="fas fa-user-plus text-white text-2xl"></i>
                </div>
                <h1 class="text-3xl font-bold text-gray-800 mb-2">Đăng ký tài khoản</h1>
                <p class="text-gray-600">Tạo tài khoản mới để bắt đầu</p>
            </div>

            <form id="registerForm" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Họ và tên</label>
                    <input type="text" class="form-input w-full px-4 py-3 rounded-lg focus:outline-none" placeholder="Nhập họ và tên" required>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Email</label>
                    <input type="email" class="form-input w-full px-4 py-3 rounded-lg focus:outline-none" placeholder="Nhập email" required>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Số điện thoại</label>
                    <input type="tel" class="form-input w-full px-4 py-3 rounded-lg focus:outline-none" placeholder="Nhập số điện thoại" required>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Mật khẩu</label>
                    <div class="relative">
                        <input type="password" id="registerPassword" class="form-input w-full px-4 py-3 rounded-lg focus:outline-none pr-12" placeholder="Nhập mật khẩu" required>
                        <button type="button" onclick="togglePassword('registerPassword')" class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-500 hover:text-gray-700">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Xác nhận mật khẩu</label>
                    <div class="relative">
                        <input type="password" id="confirmPassword" class="form-input w-full px-4 py-3 rounded-lg focus:outline-none pr-12" placeholder="Nhập lại mật khẩu" required>
                        <button type="button" onclick="togglePassword('confirmPassword')" class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-500 hover:text-gray-700">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                </div>

                <div class="flex items-center">
                    <input type="checkbox" class="rounded border-gray-300 text-blue-600 focus:ring-blue-500" required>
                    <span class="ml-2 text-sm text-gray-600">Tôi đồng ý với <a href="#" class="text-blue-600 hover:text-blue-800">Điều khoản sử dụng</a></span>
                </div>

                <button type="submit" class="btn-primary w-full py-3 text-white font-semibold rounded-lg">
                    <i class="fas fa-user-plus mr-2"></i>Đăng ký
                </button>

                <div class="text-center">
                    <span class="text-gray-600">Đã có tài khoản? </span>
                    <button type="button" onclick="showLogin()" class="text-blue-600 hover:text-blue-800 font-medium">Đăng nhập ngay</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Main Dashboard -->
    <div id="dashboardPage" class="dashboard-bg min-h-screen hidden">
        <!-- Sidebar -->
        <!--Layout menu-->
        <partial name="~/Views/Shared/QuanLy/Sidebar.cshtml" />

        <!-- Overlay for mobile -->
        <div id="overlay" class="fixed inset-0 bg-black bg-opacity-50 z-20 lg:hidden hidden" onclick="toggleSidebar()"></div>

        <!-- Main Content -->
        <div class="lg:ml-64 min-h-screen">
            <!-- Header -->
            <partial name="~/Views/Shared/QuanLy/Header.cshtml" />

            <!-- Content Area -->
            @RenderBody()
        </div>
    </div>

    <!-- Modals -->
    <!-- Room Modal -->
    <!--Mẫu thêm mới phòng-->
    <div id="roomModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden flex items-center justify-center p-4 modal-backdrop">
        <div class="bg-white rounded-xl max-w-md w-full max-h-[90vh] overflow-y-auto bounce-in">
            <div class="p-6 border-b border-gray-200">
                <div class="flex items-center justify-between">
                    <h3 class="text-lg font-semibold text-gray-800">Thêm phòng mới</h3>
                    <button onclick="closeModal('roomModal')" class="text-gray-400 hover:text-gray-600">
                        <i class="fas fa-times text-xl"></i>
                    </button>
                </div>
            </div>

            <div class="p-6">
                <form class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Số phòng *</label>
                        <input type="text" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="Nhập số phòng" required>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Loại phòng *</label>
                        <select class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" required>
                            <option value="">Chọn loại phòng</option>
                            <option>Phòng đơn</option>
                            <option>Phòng đôi</option>
                            <option>Phòng gia đình</option>
                        </select>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Diện tích (m²) *</label>
                            <input type="number" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="25" required>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Giá thuê (VNĐ) *</label>
                            <input type="number" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="2500000" required>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Mô tả</label>
                        <textarea class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" rows="3" placeholder="Mô tả chi tiết về phòng..."></textarea>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Tiện nghi</label>
                        <div class="grid grid-cols-2 gap-2">
                            <label class="flex items-center">
                                <input type="checkbox" class="rounded border-gray-300 text-blue-600 focus:ring-blue-500 mr-2">
                                <span class="text-sm">Điều hòa</span>
                            </label>
                            <label class="flex items-center">
                                <input type="checkbox" class="rounded border-gray-300 text-blue-600 focus:ring-blue-500 mr-2">
                                <span class="text-sm">Tủ lạnh</span>
                            </label>
                            <label class="flex items-center">
                                <input type="checkbox" class="rounded border-gray-300 text-blue-600 focus:ring-blue-500 mr-2">
                                <span class="text-sm">Máy nước nóng</span>
                            </label>
                            <label class="flex items-center">
                                <input type="checkbox" class="rounded border-gray-300 text-blue-600 focus:ring-blue-500 mr-2">
                                <span class="text-sm">Ban công</span>
                            </label>
                        </div>
                    </div>
                </form>
            </div>

            <div class="p-6 border-t border-gray-200 flex justify-end space-x-3">
                <button onclick="closeModal('roomModal')" class="px-4 py-2 text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 transition-colors">Hủy</button>
                <button onclick="saveRoom()" class="btn-primary px-4 py-2 text-white rounded-lg">Lưu</button>
            </div>
        </div>
    </div>

    <!-- Tenant Modal -->
    <!--Mẫu thêm mới khách thuê-->
    <div id="tenantModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden flex items-center justify-center p-4 modal-backdrop">
        <div class="bg-white rounded-xl max-w-md w-full max-h-[90vh] overflow-y-auto bounce-in">
            <div class="p-6 border-b border-gray-200">
                <div class="flex items-center justify-between">
                    <h3 class="text-lg font-semibold text-gray-800">Thêm khách thuê mới</h3>
                    <button onclick="closeModal('tenantModal')" class="text-gray-400 hover:text-gray-600">
                        <i class="fas fa-times text-xl"></i>
                    </button>
                </div>
            </div>

            <div class="p-6">
                <form class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Họ và tên *</label>
                        <input type="text" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="Nhập họ tên" required>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Số điện thoại *</label>
                            <input type="tel" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="0901234567" required>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">CCCD/CMND *</label>
                            <input type="text" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="123456789" required>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Email</label>
                        <input type="email" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="email@example.com">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Ngày sinh</label>
                        <input type="date" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Địa chỉ thường trú</label>
                        <textarea class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" rows="2" placeholder="Nhập địa chỉ thường trú"></textarea>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Nghề nghiệp</label>
                        <input type="text" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="Nhập nghề nghiệp">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Người liên hệ khẩn cấp</label>
                        <div class="grid grid-cols-2 gap-2">
                            <input type="text" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="Tên người liên hệ">
                            <input type="tel" class="form-input w-full px-3 py-2 rounded-lg focus:outline-none" placeholder="Số điện thoại">
                        </div>
                    </div>
                </form>
            </div>

            <div class="p-6 border-t border-gray-200 flex justify-end space-x-3">
                <button onclick="closeModal('tenantModal')" class="px-4 py-2 text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 transition-colors">Hủy</button>
                <button onclick="saveTenant()" class="btn-primary px-4 py-2 text-white rounded-lg">Lưu</button>
            </div>
        </div>
    </div>
</body>
</html>

